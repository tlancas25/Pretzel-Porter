node_modules/
dist/
*.log
*.bak
.DS_Store

# Editor / tool local state — never commit
.claude/
.vscode/
.idea/

# Never commit a config that may point at sensitive directories or hosts
agent.config.local.json

# Runtime working memory — created per directory by pport, not source
portmem.md
